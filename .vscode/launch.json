{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "lldb",
      "request": "launch",
      "name": "Run verus-sandbox",
      "cargo": {
        "args": ["build", "--bin", "verus-sandbox"]
      },
      "args": [],
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "verus build"
    }
  ]
}
